<div>
  <h2>Users that match your search:</h2>
    <% if @users == [] %>
        <h4>There are no users that match your search.</h4>
    <% else %>
      <% @users.each do |user| %>
      <h3><a href="/friendships/create"><%= user.username %></a><h3>
      <% end %>
    <% end %>
</div>
